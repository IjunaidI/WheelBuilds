# Project Status — Wheel Builds

> **Last verified: 2026-07-10.** This is the source-of-truth dashboard. Keep it current after
> every session (see [CLAUDE.md → Documentation workflow](../CLAUDE.md)). Backlog: [future/BACKLOG.md](future/BACKLOG.md).

## Tests
- Backend (Jest, `pnpm test:sync`): 312 passing (6 skipped — incl. 2 WB-051 integration `it.todo` stubs; +2 from WB-052 truncate-state helper)
- Backend admin (Jest, `pnpm test:admin` / `jest src/admin`): 7 passing (WB-006: `actionsForStatus`/`badgeForStatus`/`isNonTerminal` status helpers for the vendor-sync console)
- Backend wheel-size/fitment (Jest, `pnpm test:fitment` — incl. `src/modules/customer-vehicle`, whose module is UNREGISTERED since WB-076 but stays unit-tested as mothballed code): 109 passing (1 skipped — gated live-slug; +3 from G7: mergeForCustomer idempotency cases; +8 from WB-063: `oem-tire-sizes` + `canonicalize-tire-size` golden)
- Backend newsletter (Jest, `pnpm test:newsletter` / `jest src/modules/newsletter`): 8 passing (G4: email helpers + subscribe idempotency; WB-075 DOC2: atomic upsert always-201 cases)
- Backend config/lib (Jest, `pnpm test:config` / `jest src/lib`): 16 passing (WB-010/039/050: module-status, CORS, dev-max-rows, resolve-optional; WB-075 DOC4: module-status truthiness alignment)
- Storefront (Vitest, `pnpm test:unit` / `vitest run`): 317 passing (51 files) — incl. the tire store SP2/SP3 suites, WB-063 tire fitment, WB-068 multi-axis (`speed-rating-rank` golden twin, multi-axis `tire-fits-vehicle`, tire-discovery `fit_specs` post-filter), the G9 clusters (WB-070..074), and WB-076's `single-vehicle-garage` (+5; the mothballed `RoutingGarage`/`MedusaGarage` suites still run from `routing-garage.ts`)
- Storefront tsc (`npx tsc --noEmit`, non-blocking — `next.config.js` ignores build type errors): **5-error baseline** (14 → 12 when WB-075 DOC1 deleted the dead `resolveSelectedVariant`; → 5 when the `fix/auth-headers-await` hotfix awaited `getAuthHeaders()` in `customer.ts`/`orders.ts` — those 7 errors were a real dropped-auth-token bug, not drift). File list in [`storefront/CLAUDE.md`](../storefront/CLAUDE.md). Dated entries below that cite "tsc 14-baseline"/"12-baseline" predate these fixes and are accurate as historical gate records for their own session.

## Where each pillar stands

| Pillar | Maturity | State (one line) | Governing doc | Open backlog |
|---|---|---|---|---|
| Vendor import | working-with-gaps | Full fetch→stage→diff→apply; live SFTP wired; missing feed fails loud (WB-041); feed truncation explicit opt-in (WB-027); partial apply now retries safely + bounded (WB-016: partially_failed/exhausted + idempotent adopt); 6-axis variant model (WB-051) + **WB-059 finish-as-the-7th-axis (LIVE): colors collapse into one product (group key drops finish)**. Latest full prod re-import (WB-059 cutover, 2026-06-27): **1,724 groups / 29,445 variants / 16,150 stock levels / 0 errors** (was 2,670 groups pre-collapse; variants steady = no SKU loss). **Tires: WB-005 DONE + LIVE (prod cutover 2026-07-03) — group by Brand+model, canonical size = variant axis, indexed as `product_type = "tire"` with facets; ~1,000 grouped tire products applied (0 errors; 2 Toyo flotation-tire groups deferred). Storefront `/tires` discovery + tire PDP + WB-063 forward fitment all live.** **G1 async+scale — DONE 2026-07-06 (WB-011/012/013/014/015/017/018/037): manual/approve/replay triggers now run off-request via event emission + a subscriber (routes emit a `vendor-sync.*` event and return 201/202 immediately; the subscriber applies on its global container — the module cradle can't resolve core modules and `req.scope` would be disposed mid-apply); cancellation is DB-backed (`vendor_feed_run.cancel_requested_at`) and cross-process/restart-safe; apply is concurrent (`applyConcurrency`, default 8, now load-bearing) with a promise-memoized brand-collection cache; CSV parsing streams via `csv-parse` with header-derived warehouse columns (fixes a ragged-first-row stock-column loss); feed archives can durably persist to a DEDICATED PRIVATE MinIO bucket (opt-in `VENDOR_SYNC_DURABLE_ARCHIVE`, raw MinIO client, never the public File provider); a stock-only fast path + its own cron (`vendor-sync-stock-tick`, default `0 */3 * * *`) refreshes inventory between full 12h syncs without a product/variant diff.** | [done/plans/2026-05-18-vendor-sync-plan.md](done/plans/2026-05-18-vendor-sync-plan.md) · [reference/vendor-sync-implementation.md](reference/vendor-sync-implementation.md) | WB-024..WB-026 |
| Fitment (wheel-size) | production-ready | Live by_model lookups durably DB-cached + **atomic** quota guard (WB-020); reverse-fitment confirmed-models list live (WB-009). Hardened (WB-007/008/019): fractional hub bore (scaled-int), TTL + stale-while-revalidate + nightly warm cron, AbortController request timeout (slow API → 503). Migration/cron activate on next deploy. **WB-063 (2026-07-03) forward TIRE fitment: the cached `by_model` raw already carries OEM tire sizes — `extractOemTireSizes` reads `wheels[].front/rear.tire` where `is_stock===true` (canonicalized/flattened/deduped) onto `VehicleFitment.oemTireSizes`, no new API/migration. Drives a `/tires` `?fit=` filter + FITS badges + tire-PDP chip via one pure `tireFitsVehicle` verdict (shared `canonicalizeTireSize`, golden-guarded in both apps).** **WB-065 (2026-07-03) REVERSE tire fitment: `buildReverseTireFitment` (pure cache reverse, OEM-size ∩) + `service.reverseTireFitment` + `GET /store/fitment/by-tire-product` → a tire-PDP "N CONFIRMED MODELS" section (mirrors the wheel WB-009). Tires now have BOTH directions.** **WB-067 (2026-07-04): authed garage now persists `oemTireSizes` (`customer_vehicle.oem_tire_sizes` col + serialization) — tire fitment was silently broken for logged-in users (bolt patterns persisted, tire sizes didn't). WB-068 (2026-07-04): tire fitment is now MULTI-AXIS (size + load index + speed rating, meet-or-exceed) via a shared `speedRatingRank` + `extractOemTires` + per-variant Meili `fit_specs` + a `/tires` candidate post-filter — one `tireFitsVehicle(productSpecs, oemTires)` drives badge/PDP/reverse/filter. Needs the `oem_tires` migration (run) + a Meili re-sync for `fit_specs`.** | [done/plans/2026-06-23-wheel-size-fitment-hardening.md](done/plans/2026-06-23-wheel-size-fitment-hardening.md) · [in-progress/specs/2026-07-03-tire-fitment-design.md](in-progress/specs/2026-07-03-tire-fitment-design.md) · [done/specs/2026-07-03-tire-fitment-reach-design.md](done/specs/2026-07-03-tire-fitment-reach-design.md) | — |
| Garage → Vehicle cache | **retired (mothballed)** | **WB-076 (2026-07-09, client decision): the garage is RETIRED — shoppers never used accounts, so the storefront now keeps exactly ONE active vehicle in the browser's localStorage cache (`SingleVehicleGarage`: `add()` replaces; same `garage:*` keys so existing actives survived), identical for guests and logged-in users; all fitment surfaces read it via the unchanged `useGarage()`. Nothing was deleted: grep `GARAGE-DISABLED` for every seam — `RoutingGarage` moved to `routing-garage.ts` (out of the app graph, still compiled + unit-tested), GarageAuthSync unmounted, account Garage tab/route gone (404), backend `customer-vehicle` module unregistered + store routes 410 (tables NOT dropped). Restoration recipe in [done/specs/2026-07-09-retire-garage-single-vehicle-design.md](done/specs/2026-07-09-retire-garage-single-vehicle-design.md). Live-smoked 2026-07-09 (pick→fit filter→replace→clear→404→410). Pre-WB-076 history:** Guest+authed garage, single-active index. Authed mutations resolve by client_id (WB-002). G7: guest→login merge is now ONE idempotent request (`POST /store/customer/vehicles/merge` + `mergeForCustomer`), clears local only on success, with STABLE client_ids (guest vehicle id) so partial-write retries can't duplicate (WB-022); a managed account Garage tab lists/activates/removes vehicles (WB-032). **WB-073 (2026-07-07) session-integrity hardening (G9 cluster 4, 10 findings): `RoutingGarage` now keys on customer identity + a monotonic generation guard — rebuilds `remote` and re-points listeners on a customer swap so it NEVER leaks the prior customer's garage (G1/G2); authed writes are ordered (`pendingCreate`) + atomic (customer-scoped CTE activate, G4); failures are LOUD (sonner toast + per-op rollback, no silent `.catch(()=>{})`, G5) and a failed load shows a Retry state not a fake-empty garage (G6); the login merge clears only the pre-merge snapshot + drains window-adds so none are lost/duplicated (G7); non-503 fitment errors are handled (G8); merge/create batch capped at 50 (G9); an orphaned `?fit` is stripped once the garage is loaded-and-empty + GaragePill no longer flashes during the initial load (G10). No migration.** | [done/plans/2026-07-07-garage-session-integrity.md](done/plans/2026-07-07-garage-session-integrity.md) | WB-058 (plate→YMM); FU: remove-vs-pendingCreate, tire orphaned-`?fit` |
| Discovery (Meili) | production-ready | Faceted search, `?fit=` filter, FITS badges. Browse `maxTotalHits` raised to 10000 (WB-053 — activates on next Meili re-sync). WB-059 (LIVE): the finish facet is multi-valued `finishes` — a wheel shows under each normalized bucket it offers. **G5 done (WB-021/046): discovery+home Meili reads now cached 60s via `unstable_cache` (tag `discovery`, empties not cached); the dead category facet removed.** **WB-060 Option A (2026-07-01): fit-mode discovery now matches the PDP exactly — Meili can't express "SAME variant is 5x130 AND a fitting size" for multi-pattern wheels, so fit mode post-filters up to 200 candidates through the real per-variant gate (`productHasFittingVariant`, Store-API variants) before paginating; window params `fitb/fitd/fitw/fito` carry the vehicle spec. The header "FITS YOUR CAR" chip only shows when a real fit is applied (else "Select a vehicle").** **WB-005 SP2 — LIVE (prod cutover 2026-07-03): a SECOND discovery surface — a parallel `modules/tire-discovery/` module at `/tires` scoped to `product_type = "tire"`, with its own facet rail (rim/size/type/speed/load + brand/price), disjunctive-facet Meili data layer, tire product card ("N sizes", rim range), and Tires nav links. Wheel `/store` discovery + `DiscoveryProduct` untouched.** **WB-063 (2026-07-03) added tire fitment chrome to `/tires`: a `?fit=<OEM sizes CSV>` param → `tire_sizes IN [...]` Meili clause (reuses the SP2 facet, applied to hits + every disjunctive-facet query), a `FitmentSync` auto-writer + "FITS YOUR CAR" header chip + `fit=0` "Show all" escape, and per-card FITS badges — mirroring the wheel `?fit=` flow.** **WB-074 (2026-07-08) fit-mode honesty (G9 cluster 5): fit-mode facet counts tally every value from the raw hits (were collapsing to the first); a `isCapped` signal gives an honest "top 200 candidates — refine" header/mobile label + bounded pagination (was silently truncating past 200 + lying about the total); a Store-API failure now degrades to an honest UNCACHED empty (was caching an over-claiming "these fit" list) with mandatory per-variant verification; and cards no longer render the `"BLANK"` placeholder or a retired product-level finish (variant-union finish, all-black since WB-059). D3 disjunctive fit facets deferred (sanctioned). Needs a storefront rebuild.** | [done/specs/2026-05-28-fitment-ready-catalog-search-design.md](done/specs/2026-05-28-fitment-ready-catalog-search-design.md) · [done/specs/2026-07-07-discovery-honest-signals-design.md](done/specs/2026-07-07-discovery-honest-signals-design.md) | FU: BLANK at transformer; D3 caveat |
| Home | production-ready | New/brands/style rails live on Meili. G4 done: Featured Blocks render real curated products (`NEXT_PUBLIC_FEATURED_HANDLES` + Meili top-priced fallback); Build Gallery → `catalog-wall` real product mosaic; newsletter persists via a new Medusa `newsletter` module + `POST /store/newsletter`; merchandising copy → `home/data/merchandising.ts` + live brand count in metadata. No fabricated content remains. **WB-064 (2026-07-03): a live "Shop Tires" rail (newest 6 tires via `getHomeTires`→`getTireDiscoveryProducts`, `ShopTiresRow` mirroring `new-drops-row`, reuses `TireProductCard` incl. its WB-063 FITS badge) sits after "New This Week" so the landing page surfaces tires; degrades to null when none indexed.** | [done/plans/2026-06-26-home-merchandising-real-content.md](done/plans/2026-06-26-home-merchandising-real-content.md) · [done/specs/2026-07-03-home-tire-rail-design.md](done/specs/2026-07-03-home-tire-rail-design.md) | WB-057 (newsletter hardening) |
| PDP | working-with-gaps | Live price/stock, variant grid, vehicle band. Add-to-cart + Buy Now (WB-001); bolt-pattern row gates the grid (WB-003); reverse-fitment confirmed-models list (WB-009). G3 polish (WB-048/029/030). WB-056 data honesty: purchase chip uses the real `fitsVehicle` verdict (no blanket "CONFIRMED FIT"); real product weight fetched + rounded; zero/missing numeric specs hidden (no "0 lb") via `buildSpecRows`; finish swatch sized. WB-059 (LIVE): real finish selector — `finishOptions: FinishOption[]` partitions variants by raw finish; picking a finish swaps the image + per-finish size matrix + price. **2026-07-01 fitment honesty: `fitsVehicle` now requires the wheel be offered in a size/offset within the vehicle's spec window (a shared bolt pattern alone no longer reads "CONFIRMED FIT"); WB-060 fitment-aware PDP — arriving via `?fit=1` filters bolt/size/offset/color options to the active vehicle + defaults to a fitting variant, with a warned "Show all" escape (full-catalog visitors unaffected).** **WB-062 (2026-07-01) offset-label honesty: the auto-fit offset is now called "default" not "OEM" (badge "DEFAULT", "Auto-fitted · default offset") — it was never a per-vehicle factory lookup, just the wheel's first-listed offset; identifiers renamed `oemOffsetMm→defaultOffsetMm`/`isOem→isDefault`.** Remaining gaps: real photography, wishlist backend, a real per-vehicle OEM-offset centre (from wheel-size) to seed the default. **WB-005 SP3 — LIVE (prod cutover 2026-07-03): a TIRE PDP behind a `kind: "wheel"|"tire"` discriminant on the shared `/products/[handle]` route — a parallel tire detail (`mapTireDetail`, `TireProductDetail`) with a rim-diameter-chips-gate-a-size-list selector → add-to-cart (reuses the generic `addToCart`), model-level tire specs, and related-by-brand via SP2's `getTireDiscoveryProducts`+`TireProductCard`. The wheel PDP is byte-untouched beyond the discriminant seam (get-product/types/pdp-config/page).** **WB-063 (2026-07-03) added a tire-PDP fit chip: "FITS YOUR {car}" / "MAY NOT FIT" (honest three-state, judged from the product's canonical size matrix via `tireFitsVehicle`) + a fit-aware default size (defaults to a fitting OEM size when a garage vehicle is active).** **WB-065 (2026-07-03) added a tire-PDP reverse-fitment "N CONFIRMED MODELS" section (`components/tire/fitment.tsx`, mirrors the wheel WB-009) fed by `getFitmentByTireProduct` → the cached-reverse route.** **2026-07-04 polish: the tire-PDP fit chip AND the "Does it fit your ride?" band now reflect the SELECTED size (per-selection honesty, wheel WB-056 parity, bridged via a shared `selected-tire-fit` store); the tire PDP + tire store were brought to line-for-line VISUAL parity with the wheel surfaces (chrome only — tire data preserved).** | [done/plans/2026-07-01-fitment-aware-pdp.md](done/plans/2026-07-01-fitment-aware-pdp.md) · [in-progress/specs/2026-07-02-tire-pdp-design.md](in-progress/specs/2026-07-02-tire-pdp-design.md) · [done/specs/2026-07-03-tire-fitment-reach-design.md](done/specs/2026-07-03-tire-fitment-reach-design.md) | — |
| Cart / Checkout | working-with-gaps | G2 done: bare `/checkout` redirects to address (WB-033); cart qty caps at live stock (WB-034); discount remove/add no longer wipes all codes (WB-036); non-functional express-pay/Affirm env-gated off (WB-035). Gift cards deferred (WB-054); real wallet/Affirm providers still unwired. | [done/plans/2026-06-26-checkout-cart-transactable.md](done/plans/2026-06-26-checkout-cart-transactable.md) | WB-054 |
| Account / Order | working-with-gaps | **WB-076 (2026-07-09): the G7 account Garage tab/route is RETIRED** — nav links commented out, `/account/garage` 404s, `GarageManager` mothballed (see the Garage → Vehicle cache pillar). Order/checkout boilerplate copy rebranded (WB-047); residual "Medusa Store" copy elsewhere → WB-055; dead "what's next" overview cards remain (order-complete "Add to your Garage" card reworded to "Shop for your vehicle"). | [done/plans/2026-06-26-account-garage.md](done/plans/2026-06-26-account-garage.md) | WB-055 |
| Config / Infra | working-with-gaps | `medusa build` now compiles clean — 16 pre-existing TS errors that broke every deploy fixed (WB-050); railway.json builder switched to Railpack. Module status logging (WB-010); CORS fail-loud-in-prod (WB-039); per-app railway.json (WB-040); secret dump removed (WB-049). **G8-ops done: vendor-sync now operable from the Medusa admin — a "Vendor Sync" console route (run list/status-filter/trigger/approve/cancel/replay + detail drawer with counts/errors/failed groups+SKUs/replay-SKU) over the existing routes, `purge-products` deliberately not exposed (WB-006); scale-safe state wipe via shared `truncateVendorState` (WB-052); honest `Petrol` test fixtures (WB-044).** **WB-075 (2026-07-08, G9 cluster 6): the startup module-status log now mirrors `medusa-config.js`'s registration truthiness exactly (`has()` = `Boolean(env)`, no more registered-but-logged-DISABLED); the newsletter subscribe is an atomic idempotent upsert (no concurrent-dup 500); `.env.template` Meili key promise + STATUS/README/`storefront/CLAUDE.md` counts corrected; dead `resolveSelectedVariant` removed (tsc baseline 14→12).** | [../CLAUDE.md](../CLAUDE.md) | FU: module-status SendGrid axis |

## Active work
- **2026-07-10 — ✅ WB-079 bug batch B1–B11 DONE + merged to `main`** (merge `8094f10`, branch `feat/bug-batch-b1-b11`, 14 commits). Eleven audited bugs, one commit per bug: B11 (backend `tsc --noEmit` **now 0**), B7 (no literal-`0` at empty payment methods), B8 (`pickDefaultSize`→null + PDP guards variant-less products, was a crash), B4 (finish-switch derives bolt options from the active finish + filters placeholder patterns — no wrong-pattern cart, no blank chip), B1 (tire `/tires` strips orphaned `?fit` + always-removable chip), B6 (`purge-products` requires `confirm`==DATABASE_URL host), B2 (checkout Server Actions RETURN error strings so the WB-071 reassurance copy isn't prod-redacted; +`medusaError` TypeError fix; `removeCartId` best-effort post-charge), B5 (zero-file SFTP run fails loud, both `executeRun`+`runStockOnly` twins), B3 (cart created with auth headers + guest cart transferred to customer on login → orders now reach `/account/orders`), B9 (middleware exact country match), B10 (dup qty option). SDD per-bug review + fix loops (B4 WB-048 regression + B2 post-charge throw caught+fixed) + opus whole-branch review (merge: yes; 5 cross-bug interactions compose cleanly). Gate: **backend tsc 0**, vendor-sync jest 316; storefront vitest 343, tsc 5-baseline. Deferred Minors → WB-081/backlog (payment-button transport-error; `errText`/`medusaError` dedup; a cosmetic type label). **G10 status: WB-077/078/079 done; WB-080 (Stripe live/capture/US-tax, mostly ops) → WB-083 remaining.**
- **2026-07-10 — ✅ WB-078 transactional email + account recovery DONE + merged to `main`** (merge `7ec274e`, branch `feat/transactional-email-recovery`, 10 commits). The Resend SEND path was already code-complete — setting `RESEND_API_KEY`+`RESEND_FROM_EMAIL` (+ a verified Resend sender domain) on prod flips order emails on. Added: `.env.template` docs for `RESEND_*` + new `EMAIL_REPLY_TO`/`STOREFRONT_URL`; dropped the `info@example.com` reply-to; invite email rebranded Medusa→Wheel Builds; NEW shipping-confirmation template + `shipment.created` subscriber (lists only THIS shipment's `fulfillment.items`); NEW password-reset template + `auth.password_reset` subscriber; storefront forgot/reset pages + actions (no enumeration, redirect-outside-catch, Suspense toast); "Forgot password?" login link; dead account change-password form → "Send reset email" button (D4). SDD (8 tasks) + opus whole-branch review (3 Important fixes applied: partial-fulfillment shipping list, prod-localhost `STOREFRONT_URL` guard, stale password e2e). Gate: backend tsc 1 (B11 baseline); storefront vitest 337, tsc 5-baseline. **Ops pending: set `RESEND_API_KEY`+`RESEND_FROM_EMAIL`+`STOREFRONT_URL` on Railway backend + verify Resend sender domain; deferred live roundtrips (order→confirmation, ship→shipping, forgot→reset→login).** Next in G10: WB-079 (bug batch B1–B11).
- **2026-07-10 — ✅ WB-077 fitment truth v2 DONE + merged to `main`** (merge `39c273a`, branch `feat/fitment-truth-v2`, 13 commits). Fixes the user-confirmed false negatives: `normalizeByModel` now merges ALL trims + includes stock/OE rims when building windows; a three-tier verdict (`fits`/`check`/`no-fit`/`unknown`) renders out-of-window-but-bolt+bore-passing wheels as **CHECK FIT** ("aggressive — verify clearance") instead of a hard no-fit, pattern-less products as **UNKNOWN**; 0.2mm bore tolerance (golden-guarded twin across both apps); cache-key v2 re-warm. Threaded through PDP band+chip, discovery (kept+badged, D1), checkout card (dropped "Confirmed by our team", B12). SDD (10 tasks, per-task review) + opus whole-branch review caught+fixed 3 integration issues (PDP `?fit=1` hero over-claim, checkout-card metadata hydration, warm-cron v1-orphan quota treadmill). Decisions D1/D4 = defaults. Gate: backend wheel-size+warm jest 106/1skip; storefront vitest 337; tsc baselines held. **Deploy pending: backend-first → MANDATORY `wheel_size_fitment` truncate → storefront rebuild (no migration); deferred live smoke (Silverado 20x10 ET-19→CHECK, OE→FITS, 5x114.3-only→DOESN'T FIT, checkout card renders).** Next in G10: WB-078 (email/recovery), WB-079 (bug batch).
- **2026-07-10 — Launch-readiness audit + FITMENT FALSE-NEGATIVE root cause: FINDINGS LOGGED (no code changed).** The user-reported "wheels fit in real life but the site says they don't" is confirmed and root-caused (repro'd with 9 unit-test scenarios, since removed): windows come from ONE arbitrary trim (`normalizeByModel` reads `data[0]`; "Any trim" is the drawer default), stock/OE rims are EXCLUDED from window construction (an OE-replica of the factory wheel reads no-fit), and outside-window renders as a hard "doesn't fit" instead of an "aggressive fitment — verify" tier — plus a knife-edge bore compare and a product-side missing-pattern false "bolt pattern does not match". Fix plan (three-tier verdict + multi-trim window union + include stock rims + bore tolerance + symmetric unknown) with candidate ids WB-077/078/079, PLUS the full completeness/deploy audit (P0: emails unset + no password reset + manual Stripe capture + $0 US tax + 2 unrun prod scripts; bugs B1–B12; doc drift) in [future/plans/2026-07-10-launch-readiness-audit.md](future/plans/2026-07-10-launch-readiness-audit.md). **Fix SPECS drafted → [in-progress/specs/2026-07-10-launch-readiness-fixes-design.md](in-progress/specs/2026-07-10-launch-readiness-fixes-design.md): group G10 = WB-077 (fitment truth v2) / WB-078 (email + password reset) / WB-079 (bug batch B1–B11) / WB-080 (Stripe capture + US tax, BLOCKER) / WB-081 (ops hardening) / WB-082 (SEO/observability) / WB-083 (docs sweep #2) — registered in BACKLOG; 4 client decisions (D1–D4) pending.**
- **2026-07-08 — HOTFIX `fix/auth-headers-await`: login/account was fully broken (dropped auth token).** Found during pre-deploy smoke-test prep. `getAuthHeaders()` is `async` (awaits `cookies()` in Next 15); `lib/data/customer.ts` (5 sites) + `lib/data/orders.ts` (2) passed it **un-awaited** (`...getAuthHeaders()`) → a pending Promise was spread → the `Authorization: Bearer` header was dropped on every customer/order call → `getCustomer()` always returned `null` → `/account` always rendered the login form and login appeared to do nothing (order history equally dead). `cart.ts` was already correct (awaited). Fixed by awaiting at all 7 sites. These 7 were the "harmless SDK type-drift" tsc errors `storefront/CLAUDE.md` told people not to fix — the doc was wrong; fixing them dropped the tsc baseline **12→5**. Gate: storefront tsc 5, vitest 312. On branch `fix/auth-headers-await` (local, unpushed) — **needs a storefront redeploy to reach the live site, which has this bug.**
- **2026-07-08 — ✅ G9 audit-remediation EPIC COMPLETE (WB-069): all 6 clusters shipped + merged to `main` (unpushed).** WB-070 sync-lifecycle-integrity, WB-071 checkout-money-honesty, WB-072 fitment-truth, WB-073 garage-session-integrity, WB-074 discovery-honest-signals, WB-075 docs-truth-sweep — each re-verified against current `main` first, SDD-executed with per-task spec+quality review + fix loops + an opus whole-branch review per cluster. Every one of the 76 audit findings is now fixed, refuted, or corrected. Non-blocking follow-ups are recorded per-cluster in the WB-070…WB-075 backlog entries (deleteVehicle-vs-pendingCreate, tire orphaned-`?fit`, backend "BLANK" Meili filter, module-status SendGrid axis, a couple of staging psql concurrency checks). **All commits are LOCAL/unpushed.**
- **2026-07-08 — G9 cluster 6 `docs-truth-sweep` (WB-075): DONE + merged to `main` (unpushed).** SDD-executed (6 substantive tasks + epic close, per-task review with fix loops — DOC4 module-status failed review once and was corrected — + opus whole-branch review "MERGE-READY, OK to close the epic"). Ran LAST to re-baseline the counts the earlier clusters shifted. **DOC1** deleted the dead `resolveSelectedVariant` (+its test) → storefront tsc baseline **14→12**. **DOC2** rewrote the newsletter `subscribe()` as an atomic 3-CTE `knex_.raw` upsert (`target_deleted→reactivated→inserted` + `ON CONFLICT … WHERE deleted_at IS NULL DO NOTHING`) with soft-delete reactivation + a route try/catch → the documented "always 201" holds under a concurrent-duplicate race (was a list-then-create that 500'd). **DOC4** made `module-status.has()` = `Boolean(env[k])` so the startup log mirrors `medusa-config.js`'s registration truthiness for every case (`""`→disabled, whitespace→enabled) — no more "registered-but-logged-DISABLED". **DOC3** corrected the `.env.template` Meili master-key false promise; **DOC5** added a dated "superseded by WB-072" addendum to the done fitment-aware-PDP spec (history intact); **DRIFT** re-measured every suite and corrected `STATUS.md`'s Tests block, `README.md` (checkout live, tires/admin-console/reverse-fitment shipped, catalog ~1,724 wheel + ~1,000 tire groups), and `storefront/CLAUDE.md`'s 12-error baseline list — all now mutually consistent. **No migration/env; backend redeploy picks up DOC2/DOC4.** Gate: backend test:sync 312 / test:fitment 109 / test:newsletter 8 / test:admin 7 / test:config 16 + medusa build 0; storefront vitest 312 / tsc 12-baseline. Spec/plan in [docs/done/](done/plans/2026-07-07-docs-truth-sweep.md). WB-075.
- **2026-07-08 — G9 cluster 5 `discovery-honest-signals` (WB-074): DONE + merged to `main` (unpushed).** SDD-executed (5 finding-tasks + a final-review fix, per-task spec+quality review with fix loops, opus whole-branch review "MERGE-READY"). All 8 findings (D1–D8) re-verified against current `main` first (all held; the tire twin had already fixed its own D1). Storefront-only. **Fit-mode discovery honesty (D1–D4, all in `get-products.ts`):** facet counts now tally every value from the raw hits (`facetsFromHits`, was collapsing to the first — like the tire twin) (D1); a `isCapped = estimatedTotalHits > FIT_CANDIDATE_CAP` signal drives an honest "top 200 candidates — refine" header + mobile label + pagination bounded to the loaded set (was silently truncating + lying about the total) (D2); D3 (disjunctive facets) took the spec-sanctioned fallback — the single Meili query already applies every sidebar filter so in-memory disjunctive is impossible without extra round-trips, so correct D1 counts + `TODO(D3)`; a Store-API failure now RETHROWS → escapes `unstable_cache` → honest uncached empty, and mandatory per-variant verification (dropped the `!fetched` escape) kills the non-throw empty-response over-claim too (was caching an over-claiming "these fit" list) (D4). **Card + copy honesty (D5–D8):** home SEO metadata drops the numeral when the brand count is unknown (was "dealer for 0 brands" on a Meili outage) (D5); related + featured + flagship-grid cards derive `finishes` from the VARIANT-metadata union (were reading the retired product-level finish → every card black since WB-059) and omit-not-black when empty (D6); `isRealBoltPattern` gates the WB-048 `"BLANK"` placeholder at every card site incl. `hitToProduct` for the main grid (D7); the "New Drops" counter shows the real `drops.length` not a hardcoded "08" (D8). **No migration/env; storefront rebuild needed.** Non-blocking follow-ups: filter `"BLANK"` at the backend Meili transformer (root cause; + re-sync); optional filter-rail caveat for the deferred D3; the tire twin shares the D3 gap. Gate: storefront vitest 316 + tsc 14-baseline. Spec/plan in [docs/done/](done/plans/2026-07-07-discovery-honest-signals.md). WB-074.
- **2026-07-07 — G9 cluster 4 `garage-session-integrity` (WB-073): DONE + merged to `main` (unpushed).** SDD-executed (9 finding-tasks + a gate task, per-task spec+quality review with fix loops, opus whole-branch review "MERGE-READY: no Critical, no Important"). All 10 findings (G1–G10) re-verified against current `main` first (a sonner verifier confirmed all held — WB-022/032/tire-arc never touched these paths). **The root fix (G1/G2, both HIGH):** `RoutingGarage` treated "authed" as a boolean; it now keys on the customer identity + a monotonic `generation` guard — on a customer change it rebuilds `remote` (fresh load) and re-points every live listener onto the new provider, so a logout→login-as-another can't show the prior customer's garage and mounted components never go stale. **Ordered/atomic writes:** `pendingCreate` sequences create→activate→update (G3); `activate()` is one customer-scoped data-modifying CTE + 23505-retry-once + honest NOT_FOUND (G4). **Loud failures:** an `onGarageError`→sonner channel + per-op rollback + `failedCreateIds` replace the silent `.catch(()=>{})` (G5); an `isLoaded()`/`loadError()`/`retryLoad()` signal + a `loading` flag (init-true, `gen===1` first-probe, cleared on any current-gen settle) drive a 3-state GarageManager so a failed load shows Retry, not a fake-empty garage (G6). **Never-lose-a-vehicle merge:** the login merge clears only the full pre-merge snapshot + a bounded 3-round generation-guarded drain syncs window-adds (was a TOCTOU that wiped them), and killed the content-duplicate "zombie" class (G7). **Edges:** both find-by-vehicle panes handle non-503 fitment errors via a tested `resolveFitmentForVehicle` helper + non-throwing `LocalStorageGarage.update` (G8); merge/create batch capped at 50 (G9); orphaned `?fit` stripped once loaded-and-empty + GaragePill gated on `isLoaded` (G10). **No migration, no new env** (the one-active index already exists). Non-blocking follow-ups: route `deleteVehicle` behind `pendingCreate` (add-then-quick-remove); apply the G10 `?fit` strip to tire discovery; a staging psql concurrency check on the activate CTE. Gates: storefront vitest 270 + tsc 14-baseline; backend test:fitment 109 + test:sync 312 + medusa build 0. Spec/plan in [docs/done/](done/plans/2026-07-07-garage-session-integrity.md). WB-073.
- **2026-07-07 — G9 cluster 3 `fitment-truth` (WB-072): DONE + merged to `main` (unpushed).** SDD-executed (14 tasks + a caught-regression fix + a final-review fix, per-task spec+quality review, opus whole-branch review "Ready to merge: With fixes" — the one Important applied). All 17 findings re-verified against current `main` first (two parallel verifiers; all held — the tire arc only added `oem_*` columns). **Backend data integrity (B1–B8):** fitment cache key always includes the YEAR so trim slugs can't collide across generations (B1); garage hub-bore stored full-precision `hub_bore_mm_x100` + migration + guarded re-resolve backfill (B2, safety); quota exhaustion → uncached 503 (never a durable false `not_found`) while the region-probe keeps an already-found result (B4); catalog reads count quota + expire via SWR + validate + 503 (B5/B6); vehicle-update route validated (B7); atomic `ON CONFLICT` fitment upsert (B8); warm-cron trim-key loop fixed (B3). **Storefront verdict honesty (S1–S9):** `fitsVehicle` requires ONE variant to satisfy every axis + an `unknown` verdict distinct from "doesn't fit" (S1/S5); `buildFitView` pairs bore+offset per variant + trims/defaults the offset in fit-mode (S3/S4); the "confirmed models" list requires an in-window size (S2); the discovery FITS badge shows only in fit-mode (S6); honest fitment copy + removed dead CTA (S7); unique offset-chip keys (S8); YOUR VEHICLE matches year-range+trim (S9). **Review fix:** the PDP "Fits your X" band now derives fit from per-variant `buildFitView().hasFit`, so band ≡ hero ≡ fit-filter on the bore axis. **Deploy:** A1 cache re-key soft-rewarms `wheel_size_fitment` (orphan rows self-heal; optional truncate for a clean warm); A2 migration auto-runs on `db:migrate`, then run the guarded `backfill-garage-bore.ts` against prod. **No new required env.** Gates: backend test:fitment 98 + test:sync 312 + medusa build 0; storefront vitest 214 + tsc baseline-only. Spec/plan in [docs/done/](done/plans/2026-07-06-fitment-truth.md). Non-blocking follow-ups tracked in the WB-072 backlog entry. WB-072.
- **2026-07-06 — G9 cluster 2 `checkout-money-honesty` (WB-071): DONE + merged to `main` (unpushed).** SDD-executed (11 tasks + final-fix pass, per-task spec+quality review, opus whole-branch review "Ready to merge: With fixes" — the one fix applied). Re-verified all 9 PENDING findings against current `main` first (all held). Remediates: **F-A** Manual Payment (`pp_system_default`) selectable in prod → unpaid orders — hidden from customers in prod + region wires Stripe when configured + guarded `strip-manual-payment.ts` + Manual place-order button neutralized in prod; **F-B/F-I** every money display shows exact cents via a shared `formatCentsUsd` (checkout total no longer rounds then fakes `.00`; ~7 PDP/discovery/home price sites); **F-C** `placeOrder` throws on a non-order completion so a post-card-auth failure surfaces (was a silent stopped spinner); **F-E** payment-method switch re-initiates the session + button dispatches the pending session; **F-H** a real Medusa v2 conditional shipping price ($0 when item_total ≥ $199, else $10) + guarded `update-shipping-prices.ts` + aligned "$199+" copy; **F-D** single-region US lock (middleware drops IP-routing) + PDP/featured price the route region; **F-F** apartment/unit (`address_2`) carried through checkout; **F-G** invalid promo codes now show an inline error. **No migration.** Deploy: run `strip-manual-payment.ts` + (after a dev-DB dry run) `update-shipping-prices.ts` against prod (both `--confirm-host`-guarded); staging-smoke the shipping thresholds + prod Manual absence. Gates: storefront vitest 209 + tsc baseline-only, backend test:sync 312 + medusa build exit 0 (`build:next` deferred — needs a live backend). Spec/plan in [docs/done/](done/plans/2026-07-06-checkout-money-honesty.md). WB-071.
- **2026-07-06 — G9 cluster 1 `sync-lifecycle-integrity` (WB-070): DONE + merged to `main` (unpushed).** SDD-executed (10 tasks + a final-fix pass, per-task spec+quality review, opus whole-branch review "Ready to merge: Yes"). Remediates the **9 CONFIRMED vendor-sync findings + folded mediums #11/#16** as four seams: **(A) stock honesty** — content hash now counts per-warehouse stock (F11); zero-out reconciles against real Medusa levels not the overwritten `normalized` snapshot (F1, phantom-stock/oversell); the stock pass surfaces its errors into the run (→ `partially_failed`, `failed_part_numbers`) AND owns the settled `content_hash` (group writes a `""` sentinel, stock settles on success → a stock failure re-selects the part) (F5). **(B) adoption honesty** — re-listed discontinued groups republish (republish is the LAST mutation = idempotent commit marker) + `refreshReListedVariants` clears discontinued flags/refreshes price (F2/F4); adoption creates missing variants + throws rather than persist null-variant "zombie" rows (F3). **(C)** emit `product.updated` after variant-only mutations so Meili reindexes prices/facets (F6). **(D) lifecycle guards** — dry runs get `mode:"dry"` so they don't skip the next real sync (F7); `awaiting_approval` blocks new FULL runs, approve/replay/replaySku take a vendor lock, approve re-validates status + refuses a superseded run (F8/F9/F16). Review fast-follows fixed: stock-only cron stays unblocked during a park (mode-aware guard); a missing `inventory_item_id` now surfaces as an error. **No migration** (`mode:"dry"`/`""`/`"superseded"` are values). **Deploy: the first post-deploy FULL sync re-applies the whole catalog once** (hash format changed — idempotent, run off-peak). Gates: test:sync 312 pass, tsc baseline-only, medusa build exit 0. Spec/plan in [docs/done/](done/plans/2026-07-06-sync-lifecycle-integrity.md). WB-070.
- **2026-07-06 — Full done-specs audit (code + business logic): FINDINGS LOGGED → G9/WB-069.** A 27-reviewer multi-agent audit of every done plan/spec (24 units + 6 cross-cutting domains, run wf_7e98d308-058 across 8 usage windows) produced **116 raw → 76 unique findings, none previously tracked**. Adversarial verification ran for 12 before usage limits: **9 CONFIRMED (unanimous 3-lens panels) — all HIGH, all vendor-sync lifecycle**: phantom warehouse stock after per-warehouse sellouts (oversell risk), discontinued-reappear products stuck DRAFT forever, zombie null-variant state rows, re-listed variants keeping stale prices, stock-pass errors invisible to retry, price changes never re-indexed in Meili, dry-run blocking the next real sync, stale-approval catalog rollback, no approve/replay concurrency guard. 47 findings pending verification (incl. placeOrder swallowing completion errors post-auth, Manual Payment selectable in prod, fitment cache-key year drop). Theme + 4 raw finding logs in [future/plans/2026-07-06-audit-remediation-theme.md](future/plans/2026-07-06-audit-remediation-theme.md); umbrella backlog item WB-069; six remediation clusters defined (sync-lifecycle-integrity first). **Caveat: finder passes span 07-04→06 snapshots — G1 + WB-063..068 merged mid-audit, so pending findings need re-checking against current main at spec time.** No code changed by the audit itself.
- **2026-07-06 — G1 vendor-sync productionization (async + scale): DONE + merged to `main` (unpushed).** SDD-executed (10 tasks, spec+quality review per task + final opus whole-branch review "Ready to merge: Yes" after fixing a Critical). Closes all 8 G1 backlog items: **WB-011** manual trigger (`POST /runs`) now returns 201 immediately, then a subscriber (`src/subscribers/vendor-sync-run.ts`) applies off-request on its global container — the route emits a `vendor-sync.execute` event rather than running in-process, because the module cradle can't resolve core modules (a Critical caught in the final whole-branch review + fixed); **WB-012/013** approve/replay/replay-sku now return 202 immediately via the same event→subscriber pattern; **WB-014** apply is genuinely concurrent (pure `mapWithConcurrency`, `VENDOR_SYNC_APPLY_CONCURRENCY` default 8 — no longer dead config) with a promise-memoized brand-collection cache; **WB-015** CSV parsing streams via `csv-parse` with header-derived warehouse columns (fixed a silent stock-column-loss bug found in review); **WB-017** feed archives can durably persist to a DEDICATED PRIVATE MinIO bucket, opt-in via `VENDOR_SYNC_DURABLE_ARCHIVE` (raw MinIO client — never the shared File provider, which forces public-read); **WB-018** a stock-only fast path + its own cron (`vendor-sync-stock-tick`, `VENDOR_SYNC_STOCK_CRON` default `0 */3 * * *`) refreshes inventory between full 12h syncs; **WB-037** cancellation is DB-backed (`cancel_requested_at`) and cross-process/restart-safe. Deploy notes: the foundation migration (adds `cancel_requested_at` + `mode` to `vendor_feed_run`) runs automatically on `db:migrate`; the new stock cron self-registers, no manual activation step; **no new REQUIRED env** (all new vars are optional with safe defaults, durable archive stays off unless explicitly opted in). Gates: `test:sync` green throughout, `tsc` baseline-only, `medusa build` exit 0. Spec/plan in [docs/done/](done/plans/2026-07-05-vendor-sync-productionization.md).
- **2026-07-04 (cont.) — Tire fit-chip honesty + tire↔wheel VISUAL parity: DONE + pushed to `main`.** Post-WB-068 polish from live testing (direct, not subagent-driven — small surgical fixes). **(1) Fit honesty:** both tire-PDP fit surfaces now reflect the SELECTED size, not "does ANY size fit". The hero purchase chip keys on `selectedSize` (mirrors the wheel WB-056 `variantFitsVehicle`), and the "Does it fit your ride?" fitment band tracks it too via a new zero-dependency `lib/stores/selected-tire-fit.ts` `useSyncExternalStore` bridge (hero publishes on selection, the sibling band subscribes) — so hitting "Show all" and picking a non-OEM size honestly flips BOTH to "MAY NOT FIT"; the "YOUR VEHICLE" row highlight is decoupled so it stays marked regardless of selected size. **(2) Visual parity:** a component-by-component diff (two fan-out read agents over all 24 pairs) aligned the tire store + tire PDP to their wheel counterparts (chrome only — tire data preserved). Tire store: filter rail gains the Vehicle band + the fit-aware "no fitting tires → See all tires" empty state; the product card drops the corner tire-type chip so the FITS badge sits in the wheel's top-right corner; page/rail skeletons match. Tire PDP: hero re-ordered to fit-banner → purchase-panel (now owns brand/name/price/description/chip/`Separator`/qty+cart+heart/buy-now/trust) → size-picker; gallery gains the `wheel-glow`; size-picker gains the bold Display stat tiles + hover tooltips; specs a voiced title; fitment a footer note. Gates: storefront tsc 14-baseline + vitest 200. Commits `3257b64`/`3f7e573`/`3feee8b`. Intentional data-driven gaps kept: no specs `spotlight` sub-paragraph, no "nothing fits" red band (tires never filter to empty).
- **2026-07-04 — WB-067 (authed-garage tire fitment fix) + tire-PDP/nav/hero fixes + home TIRES band + WB-068 (multi-axis tire fitment): DONE + merged to `main` + pushed 2026-07-04.** Live-debugging session. **Fixes:** tire PDPs rendered as WHEEL PDPs (blank image + zeroed specs) because `getProductByHandle` didn't fetch `product.metadata` → the `product_type` discriminator always failed — fixed by fetching metadata; nav highlighted Wheels always (server component, hardcoded) → now path-derived; hero "Find My Fit" CTA dropped on bare `/store` → carries the fit; the Wheels|Tires toggle removed → page-aware routing via a `fitment-context` surface store; a bold home TIRES band replaced the quiet rail; the "no fitment" toast made page-aware; the garage backfills OEM sizes on re-select; the tire PDP now filters sizes to the vehicle in fit mode with a "Show all" escape (WB-060 parity). **WB-067:** the authed `customer_vehicle` table + `medusa-garage` dropped `oemTireSizes` (wheels persisted bolt patterns, tires got nothing) → added the `oem_tire_sizes` column (migration run) + serialization. This session's `db:migrate` ALSO ran the long-pending WB-007 bore migration + G4 newsletter table (init-backend had been skipping migrations on the seeded prod DB). **WB-068 (10-task subagent-driven epic, opus final):** tire fitment is now multi-axis — size + load index + speed rating (meet-or-exceed; missing data passes). Shared `speedRatingRank` (golden, H between U/V); backend `extractOemTires`→`oemTires`, multi-axis reverse, per-variant Meili `fit_specs`, `customer_vehicle.oem_tires` (migration run); storefront one multi-axis `tireFitsVehicle(productSpecs, oemTires)` drives badge/PDP chip/PDP fit-mode filter/reverse, and `/tires` fit mode post-filters candidates over `fit_specs` (no Store-API round-trip — lighter than wheel WB-060). Gates: backend test:fitment 81 (serial) + test:sync green, storefront tsc 14-baseline + vitest 200. **Deploy: needs the metadata-fetch + serialization redeploy, storefront rebuild, and a Meili re-sync for `fit_specs` (post-filter treats empty fit_specs as PASS pre-resync).** Specs/plans in [docs/done/](done/plans/2026-07-04-multi-axis-tire-fitment.md).
- **2026-07-03 (cont.) — WB-065 reverse tire fitment + WB-066 vehicle funnel into tires: DONE (branch `feat/tire-fitment-reach`).** Two tire-fitment reach gaps closed, brainstorm→spec→plan→subagent-driven (6 tasks + per-task spec+quality reviews). **WB-065:** the tire analog of the wheel WB-009 — `buildReverseTireFitment` (pure reverse over the cached `wheel_size_fitment` rows: match cached vehicles whose `extractOemTireSizes(raw)` ∩ the product's canonical `tire_sizes`, identity via the reused `extractVehicleIdentity`) + `service.reverseTireFitment` + `GET /store/fitment/by-tire-product` (degrades `{vehicles:[]}`, never 503); the tire PDP loader populates `TireProductDetail.fitment`; `components/tire/fitment.tsx` (mirrors the wheel fitment section, boolean `tireFitsVehicle` verdict) renders "N CONFIRMED MODELS" + the active-vehicle band. No new wheel-size API/quota/migration. **WB-066:** a "Shop for: Wheels | Tires" toggle (default Wheels) on the YMM + garage panes + a pure `fitmentDestinationUrl` builder — Wheels → `/store?fit=<boltPatterns>`, Tires → `/tires?fit=<oemTireSizes>` (pre-fitted first paint); the garage `update()` now persists `oemTireSizes` too. Every existing fitment/toast branch preserved. Gates: backend `test:fitment` 71 pass/1 skip, storefront vitest 171 pass (32 files), tsc 0-new. Known cosmetic follow-up: the "No fitment data" toast is wheel-flavored regardless of target. Spec/plan in [docs/done/](done/plans/2026-07-03-tire-fitment-reach.md).
- **2026-07-03 (cont.) — WB-064 home "Shop Tires" rail: DONE (branch `feat/home-tire-rail`).** One new home section so the landing page shows tires: a throw-safe `getHomeTires(limit)` wraps `getTireDiscoveryProducts` (`sort:"newest"`, `EMPTY_TIRE_FILTERS`); `ShopTiresRow` (async server component mirroring `new-drops-row`) renders the newest 6 via the existing `TireProductCard` grid after `<NewDropsRow/>`, degrading to `null` when empty. Reuses the WB-063 `TireFitBadge` for free. No new facet/card/test, no backend change. brainstorm→spec→plan→subagent-driven (1 task + spec+quality review "Approved"). Gate: storefront tsc 0-new on the 3 touched files, `/` compiles. Spec/plan in [docs/done/](done/plans/2026-07-03-home-tire-rail.md).
- **2026-07-03 — TIRE STORE LIVE + WB-063 forward tire fitment: DONE.** Two things landed. **(1) WB-005 prod cutover RUN — the tire store is live.** All 3 sub-projects (SP1 backend grouping+indexing, SP2 `/tires` discovery, SP3 tire PDP) merged to `main`; the cutover applied the `wheelpros-tires` feed (dry-run staged **4,109 new**; apply built ~1,000 grouped Brand+model tire products with per-warehouse inventory + `product_type = "tire"` Meili docs/facets). One env gotcha fixed mid-cutover: `VENDOR_WHEELPROS_TIRE_SFTP_PATTERN` initially carried the wheel pattern → "no feed file found" until corrected. Two Toyo flotation-tire groups (`OPMT 33x R15LT C6`, `OPMT 38x R18LT D8`) failed to parse — deferred polish. User-verified `/tires` + tire PDPs render. **(2) WB-063 forward tire fitment** — subagent-driven (8 tasks + per-task spec+quality reviews). Substitutes *tire size* for *bolt pattern* through the proven wheel fitment pipeline: `extractOemTireSizes` reads the vehicle's factory tire sizes from the **already-cached** `by_model` raw (no new API/migration) → `VehicleFitment.oemTireSizes` → garage vehicle → a `/tires` `?fit=` filter (`tire_sizes IN [...]` Meili clause reusing the SP2 facet, threaded through hits + every disjunctive-facet query) with a `FitmentSync` auto-writer + "FITS YOUR CAR" header chip + `fit=0` escape, per-card FITS badges, and a tire-PDP "FITS YOUR {car}"/"MAY NOT FIT" chip + fit-aware default size. One pure `tireFitsVehicle` verdict drives badge+filter+chip; a shared `canonicalizeTireSize` (byte-identical to the vendor-sync `canonicalTireSize`) is golden-guarded (`fixtures/tire-size-canonical-golden.json`) in both apps. Wheel fitment path unchanged (only additive optional `oemTireSizes`). Gates: backend `test:fitment` **66 pass / 1 skip**, storefront **vitest 168 (31 files)**, tsc 0-new on the tire/fitment surface. On `feat/tire-fitment` (pending opus final review + merge). Forward-only (reverse "N vehicles per tire" deferred; OEM-only; front+rear flattened). Spec/plan in [docs/in-progress/](in-progress/plans/2026-07-03-tire-fitment.md).
- **2026-07-02 (cont.) — TIRES (G6 · WB-005) Sub-project 3: tire PDP — DONE on branch `feat/tire-pdp` (unmerged); WB-005 now CODE-COMPLETE.** Final tire sub-project (design → spec → plan → subagent-driven, 6 tasks + per-task spec+quality reviews). The shared `/products/[handle]` route branches on a new `kind: "wheel" | "tire"` discriminant → a parallel tire detail, leaving the wheel PDP byte-untouched beyond a 4-file seam (`get-product.ts` reads `product_type` → `mapTireDetail`; `types.ts` adds the discriminant + `TireProductDetail`/`AnyProductDetail`; `pdp-config.ts` adds `DEFAULT_TIRE_QTY`; `page.tsx` branches on `kind`). New parallel code: `mapTireDetail` + pure helpers (`buildTireSizeOptions`/`classifyTireType`/`buildTireSpecRows`, unit-tested); a `TireHero` where **rim-diameter chips gate a size list** (auto-hidden at ≤1 rim), a selected-size spec readout, and **Add-to-cart / Buy Now reusing the generic `addToCart`**; model-level `TireSpecs`; a `TireBreadcrumb` (→ `/tires`); a related-tires section reusing SP2's `getTireDiscoveryProducts` + `TireProductCard`; and a `TireDetailTemplate` with **no fitment section**. Gates: storefront **vitest 153 (28 files, +12 tire-pdp)**, **tsc back to the 14-error baseline** (the 2 transient union-narrowing errors from the loader widening resolved cleanly by the `kind` branch), `/products/[handle]` compiles. **DEFERRED → the cutover:** a live tire PDP needs tire products in the DB (no tire handle resolves until the tire feed applies). Rolled-up minor: the add-to-cart toast names the size, not the model. **WB-005 is now code-complete across SP1+SP2+SP3; the only remaining step is the prod cutover (tire feed apply → the tire store goes live).** Spec/plan in [docs/in-progress/](in-progress/plans/2026-07-02-tire-pdp.md).
- **2026-07-02 (cont.) — TIRES (G6 · WB-005) Sub-project 2: tire discovery — DONE + merged to `main` (unpushed; opus final review "READY TO MERGE").** Second of three tire sub-projects (design → spec → plan → subagent-driven, 9 tasks + per-task spec+quality reviews). A **parallel `modules/tire-discovery/` module** + a `/tires` route give tires their own faceted browse surface WITHOUT touching the live wheel discovery, `DiscoveryProduct`, home rails, or PDP (the owner chose the parallel module over a shared generic engine to avoid the 16-consumer blast radius). Pieces: `TireDiscoveryProduct` type + `parseTireQueryFromSearchParams` + a `"tire"`-discriminated cache key; a disjunctive-facet Meili data layer (`getTireDiscoveryProducts`, scoped `product_type = "tire"`, `unstable_cache` + throw-safe empty) reusing only the pure `lit` escape helper from the wheel module; a route-agnostic client URL-query hook; a tire product card ("N sizes · rim range", "from $X", tire-type badge, NO finish/fitment chrome); a facet rail (Brand · Rim · Size · Tire type · Speed · Load · Price, Vehicle band removed); header/active-chips/empty-state/pagination with all fitment stripped; the template + skeleton; and `Tires` nav links (desktop + mobile). Gates: storefront **vitest 141 (24 files, +16 tire)**, tsc **0-new tire errors**, `/tires` route compiles (full `next build` is env-blocked by a PRE-EXISTING backend-dependent `generateStaticParams` in collections/categories — proven via git-stash A/B, not a tire regression). **DEFERRED → pre-deploy:** a live `/tires` browse (needs the tire feed applied + Meili indexed). Rolled-up minors for the final review: a dead import, the loading skeleton still showing a phantom wheel "Vehicle band", a `TIRE_TYPE_LABELS` triplicate. **Remaining for the tire store: SP3 (tire PDP) + prod cutover.** Spec/plan in [docs/in-progress/](in-progress/plans/2026-07-02-tire-discovery.md).
- **2026-07-02 — TIRES (G6 · WB-005) Sub-project 1: backend grouping + indexing — DONE + merged to `main` (unpushed; opus final review "READY TO MERGE").** First of three sub-projects for the shoppable tire store (brainstorm → spec → plan → subagent-driven build, 11 tasks, per-task spec+quality reviews). Tires now collapse into grouped, multi-variant, indexed products: a pure model extractor scrapes the model from the free-text `PartDescription` (`wheelpros-tires/model-key.ts` + golden fixture), the group key becomes `Brand|<model>` with a per-SKU fallback (`group-key.ts`), the canonical **size** (`305/45R22 118S`) is the single variant axis (`tire-facets.ts` + `tire-grouping.ts`, mirroring the wheel machinery), `applyNewTireGroup` builds a real N-variant product (+ a changed-group add-path for new sizes), and `buildSearchDocument`'s `buildTireDocument` branch + `medusa-config.js` facets index them as `product_type = "tire"` (size/rim/width/aspect/load/speed/type). **Two plan bugs the review loop caught + fixed:** (a) my `external_id` adoption fork over-generalized the `sku:`-strip to WHEELS → would have broken per-SKU-fallback wheel idempotency (now branches by productType); (b) a self-contradictory fallback-test example + a rim-primary size comparator that failed its own test (→ width→aspect→rim). Gates: backend `test:sync` **288 passed / 6 skipped**, tsc no-new (1 pre-existing admin-route baseline), `medusa build` exit 0. **DEFERRED → pre-deploy:** the live `pnpm vendor-sync:dry-run wheelpros-tires` + a dev-DB apply (needs a real DB; not run from this session to avoid touching prod staging tables). **Remaining for the tire store:** SP2 (tire discovery `/tires` route) + SP3 (tire PDP) + the prod cutover (deploy → backend restart for Meili tire settings → tire feed apply, WB-059 cutover shape). Spec/plan in [docs/in-progress/](in-progress/plans/2026-07-02-tire-store-backend.md).
- **2026-07-01 (cont.) — Fitment consistency + navigation loading + offset-label honesty: DONE on `main` (unpushed).** Completes the WB-060 arc from live testing. **(1) Discovery↔PDP consistency — WB-060 Option A.** A wheel could appear in the fit results yet read "doesn't fit" on its PDP, because discovery filtered by bolt pattern only (Meili product-level facets can't express "the SAME variant is 5x130 AND a fitting size" for multi-pattern wheels). New pure `productHasFittingVariant(variants, vehicle)` (mirrors the PDP per-variant gate, 8 tests) + a fit-mode post-filter in `get-products.ts`: pull ≤200 bolt-pattern candidates from Meili → fetch their real variants via the Store API → drop any product with no genuinely-fitting variant → paginate + recompute facets in memory (degrades to coarse on fetch error). `DiscoveryQuery.vehicleFitment` + `fitb/fitd/fitw/fito` + `discoveryCacheKey` carry the vehicle spec server-side. **(2) FitmentSync async-window bug.** `useGarage`'s snapshot memo compared only vehicle id/count, so the YMM flow's in-place `update(id, {windows})` (windows land a beat after the vehicle is added) never re-rendered — window params only reached the URL on refresh/car-switch. Memo now keys on a full content signature. **(3) Navigation loading bar — WB-061.** App-wide `@bprogress/next` top bar (WB orange, 3px, `shallowRouting`) in the root layout — fixes the "press fit, 3–4s of nothing" dead zone (same-route `?fit=` change renders softly with no `loading.tsx`). Fit buttons + Discovery query hook + FitmentSync switched to bprogress's `useRouter` so programmatic pushes fire it; `<Link>` clicks fire automatically. **(4) Chip honesty.** The discovery "FITS YOUR CAR" header chip now shows only when a real fit is applied (else "Select a vehicle") — not merely because a vehicle exists (was wrong at `fit=0`/no-data). The PDP purchase-panel chip now reflects the SELECTED variant (`variantFitsVehicle`) not the whole product, so after "Show all" it honestly flips to "MAY NOT FIT" when you pick a non-fitting size/offset/colour. **(5) Offset-label honesty — WB-062.** Renamed the auto-fit "OEM" offset → "default" (copy + `oemOffsetMm→defaultOffsetMm`/`isOem→isDefault`); it was never a per-vehicle factory match. storefront vitest 125/125, tsc 0-new. **Deploy note:** WB-061 adds a dependency + root-layout provider → storefront rebuild required; dev needs a full restart.
- **2026-07-01 — Pre-launch smoke-test fixes + WB-060 fitment-aware PDP: DONE + merged to `main` (unpushed).** During live smoke-testing of the deployed storefront, four blockers were found + fixed (systematic-debugging, root-caused from code): **(1) register crash** — `signup()` returned the customer object into `<ErrorMessage>` (React #31); now returns undefined + redirects to the dashboard, `ErrorMessage` only renders strings. **(2) empty checkout delivery step** — the seeded fulfillment service zone covered Europe only (gb/de/dk/…); US carts matched no shipping option. Fixed `seed.ts` (add us/ca) + shipped a guarded `update-shipping-zones.ts` (`fulfillment.createGeoZones`, `--confirm-host`) that the user ran against prod → delivery now works. **(3) login did nothing on correct creds** — `login()` set the cookie + `revalidateTag` but never navigated; the account layout's parallel-route slot didn't re-render. Now `redirect()`s to `/account` on success (outside try/catch), countryCode via hidden field; `signup` mirrors it. **(4) `fitsVehicle` over-claimed** — `fits = hardGatesPass` ignored the size/offset window, so any shared bolt pattern read "CONFIRMED FIT"; now requires the wheel be offered in an in-window size (else honest "MAY NOT FIT"), and won't claim a confirmed fit with no window on file. Then **WB-060 fitment-aware PDP** (subagent-driven, 3 tasks + per-task reviews + opus final "ready to merge"): pure `buildFitView(product, vehicle)` → fitting bolt/size/offset/**color** subsets; discovery card carries `?fit=1` in fit mode only; the hero filters the pickers + re-snaps to a fitting variant with a `FitBanner` "Show all" escape gated by a shadcn Dialog confirmation; hero wrapped in `<Suspense>` (uses `useSearchParams`). Storefront-only; no backend/migration for WB-060. Gates: storefront vitest 117/117, tsc 0-new. **Stripe test path confirmed working end-to-end** by the user (card → order → paid; the only gap is order-confirmation email, which needs `RESEND_API_KEY` + `RESEND_FROM_EMAIL` on the backend). Spec/plan in [docs/done/](done/plans/2026-07-01-fitment-aware-pdp.md).
- **G5 + G8-ops — Wheel discovery & vendor-sync ops: DONE + merged to `main` (unpushed) 2026-06-28.** Five wheel-related hardening items, subagent-driven (6 tasks + per-task reviews + opus final review "Ready to merge: Yes"). **WB-021:** discovery+home Meili reads wrapped in Next `unstable_cache` (60s, tag `discovery`) keyed by a pure order-independent `discoveryCacheKey`; the inner fetch throws so the `try/catch → emptyResult` sits OUTSIDE the cache (empties never cached, self-heal next request); PDP still reads live so 60s listing staleness is fine. **WB-046:** removed the dead category facet (no source in feed/transformer/index) across types/facets/filter-rail + all consumers + the `DiscoveryProduct.categories` type-ripple; `STYLE_DEFS` untouched. **WB-006:** a Medusa admin route (`src/admin/routes/vendor-sync/page.tsx`, sidebar "Vendor Sync") over the 8 existing routes — run list/status-filter, trigger dry-run, status-gated approve/cancel/replay (pure jest-tested helpers + `test:admin`), confirm prompts, polling while non-terminal, detail drawer (counts/errors/failed groups+SKUs, fetches fresh detail on open) + replay-SKU; `purge-products` deliberately NOT exposed. Build-gated (`medusa build` clean); added `@medusajs/icons`@2.13.6 + `@medusajs/ui`@4.1.6 as direct backend deps (surgical lockfile change). **WB-044:** renamed misleading `Teraflex`→`Petrol` fixtures. **WB-052:** shared `truncateVendorState(knex)` helper; dev-wipe + truncate-state delegate (kills the `WHERE id IN (372k)` knex overflow). Gates: backend vendor-sync 244/6-skip, admin 7, storefront vitest 110, `medusa build` clean, storefront tsc 0-new (2 baseline `resolve-variant.test.ts` errors pre-date this branch). **No DB migration; no prod data touched.** Tires (WB-005), pricing/de-hardcode (WB-024/025/026), and reply-to/shipping (WB-031) deferred. Spec/plan in [docs/done/](done/plans/2026-06-28-wheel-discovery-vendor-ops.md).
- **WB-059 — Finish as a variant axis: DONE + LIVE (merged + prod cutover run 2026-06-27).** Wheels identical except color collapse into ONE product with selectable finish variants. Drop finish from the wheel group key (`Brand|DisplayStyleNo`) → finish is the **7th variant axis** (raw label, blank→`—` sentinel; `variantAxisKey`↔`axisKeyFromMetadata` byte-identical so the incremental add-path dedupes after re-import); `finish`+`image_url` move to **variant** metadata; product `images` = union of finish images, `thumbnail` = representative finish. Meili emits multi-valued normalized `finishes`; index settings `finish`→`finishes`. Discovery `DiscoveryProduct.finishes: Finish[]` (a wheel shows under each bucket). PDP: `buildFinishOptions` partitions variants by raw finish → `FinishOption[]`; the finish selector swaps image + per-finish size matrix + price; single-finish products hide the selector. No DB migration; old per-finish URLs 404. Subagent-driven (10 tasks + per-task reviews + opus final review "Ready to merge: Yes"; a full-suite gate caught one stale `wheel-normalize` assertion → fixed). backend vendor-sync 242 / storefront 105. **Phase 5 cutover executed** (purge-products → `vendor-sync-truncate-state.ts` → re-import → backend restart for Meili `finishes` settings): collapsed **2,670 → 1,724 groups / 29,445 variants / 16,150 stock levels / 0 errors**; user-verified live (multi-color model = one product w/ finish selector; finish facet works; old `…-<finish>` URLs 404). Spec/plan in [docs/done/](done/plans/2026-06-27-finish-as-variant.md).
- **G7 — Account & garage (WB-032/022/045): DONE + merged to `main` (unpushed).** WB-032: a new auth-guarded `@dashboard/garage/page.tsx` route renders `GarageManager` (client component over `useGarage()`, legacy account styling) — list / set-active / remove saved vehicles + an "Add a vehicle" button that reuses the canonical YMM search drawer; a "Garage" link (new Car icon) sits in the account nav (desktop + mobile). WB-022: the guest→login merge is now ONE idempotent `POST /store/customer/vehicles/merge` (`mergeForCustomer` loops the existing idempotent `createForCustomer`, returns only the caller's list); the storefront sends one batch and clears the local garage ONLY on success (failure retains local + retries next auth sync). **Opus-final-review fix:** `planMerge` now returns full `Vehicle[]` so the guest vehicle's STABLE local id is the `client_id` — a partial-write retry re-sends the same `(customer_id, client_id)` and the guard absorbs it instead of duplicating. WB-045: removed the dead "search by license plate" stub (→ WB-058 for a real provider). Subagent-driven (5 tasks + per-task reviews + opus final review "With fixes" → 1 Important fixed). backend customer-vehicle 9 tests / storefront 102. **No DB migration** (the `customer_vehicle` table already exists). Live smokes (merge persists/idempotent/401, guest→login all-merged + forced-failure retry, Garage tab CRUD, `/account/garage` 404 logged-out) deferred → pre-deploy.
- **G4 — Home & merchandising real content (WB-004/023/028): DONE + merged to `main` (unpushed).** Featured Blocks now render real products — `getFeaturedProducts` pulls a curated `NEXT_PUBLIC_FEATURED_HANDLES` (CSV) list exact via the Medusa Store API, falling back to top-priced wheels from Meili; pure unit-tested `selectFeatured` (merge/order/dedup/cap). Build Gallery's fictional "14.2K community posts" mosaic became `catalog-wall` — a real product mosaic from the already-fetched home catalog (real thumbnails, PDP links, honest "LATEST ARRIVALS" copy). Newsletter persists for real: a new Medusa `newsletter` module (`newsletter_subscription` table, unique-email index partial on `deleted_at IS NULL`, idempotent `subscribe`, pure jest-tested `normalizeEmail`/`isValidEmail`) behind a public `POST /store/newsletter` that always answers `201 { subscribed:true }` (no membership leak); the storefront server action replaces the fake `setTimeout`. Merchandising copy → `home/data/merchandising.ts` config; home `generateMetadata()` uses the live brand count (fabricated "40+" gone). Every home render path is throw-safe (degrades to null/empty); no fabricated content remains. Subagent-driven (6 tasks + per-task reviews + opus final review "Ready to merge: Yes"; one Important throw-safety fix landed). storefront 100 tests / backend newsletter 5. **Pending deploy:** the hand-authored `Migration20260626120000` creates the newsletter table on `db:migrate`. Newsletter hardening (unsubscribe/rate-limit/double-opt-in) → WB-057. Live smokes (POST persists+idempotent, Featured curated/fallback render, catalog-wall PDP links, live metadata count) deferred → pre-deploy.
- **WB-056 — PDP data honesty & fitment polish: DONE + merged to `main` (unpushed).** The purchase-panel chip now uses the real `fitsVehicle(product, active).fits` (same fn as the Fitment section), so it shows "CONFIRMED FIT" only when the wheel actually fits — "MAY NOT FIT" otherwise (was a blanket false claim for any garage vehicle). The PDP product query now fetches `+weight` (the importer already persists it from the feed's `ShippingWeight`; it just wasn't requested) and rounds it; a pure unit-tested `buildSpecRows` hides any zero/missing numeric spec (weight/load/bore, finishOptions=1) instead of a fake "0 lb"/"0 mm" (variant-picker weight stat + tooltip gated too). The finish swatch is a sized 96px square (80px wheel), not a 72px wheel in an empty box. Storefront-only — no backend/migration/re-import. Subagent-driven (3 tasks + reviews + opus final review). storefront 95 tests.
- **G2 — Checkout & cart transactable (WB-033/034/035/036/047 + WB-053): DONE + merged to `main` (unpushed).** Bare `/checkout` now `redirect()`s to `?step=address` (WB-033). Cart line qty caps at live `inventory_quantity` via pure `maxSelectableQty` (WB-034). The discount remove/add wipe-all bug fixed via pure `retainedPromoCodes` (`!is_automatic && code != null`); three dead v1 gift-card stubs deleted (WB-036; gift cards v2 → WB-054). Non-functional express-pay/Affirm chrome env-gated behind two default-OFF `NEXT_PUBLIC_*` flags — hidden by default, **changing a flag needs a storefront rebuild** (WB-035). "Medusa Store"/onboarding-CTA boilerplate removed from order/checkout (WB-047; residual copy elsewhere → WB-055). Meili `maxTotalHits` raised to 10000 (WB-053 — **activates on next Meili re-sync/deploy**). Subagent-driven (5 TDD tasks + per-task reviews + opus final review "ready to merge"; one minor hardening landed). storefront 89 tests. Live checkout smokes deferred to pre-deploy.
- **G3 — PDP correctness & polish (WB-048/029/030): DONE + merged to `main` (unpushed).** WB-048: pure `isRealBoltPattern` drops placeholder "BLANK"/empty bolt patterns at the loader so they're never a clickable grid-gating chip (placeholder sizes still reachable via the all-sizes fallback; bolt-pattern row hidden when ≤1 real pattern). WB-029: `pdp-config.ts` de-hardcodes qty default / low-stock threshold / ship copy (env-overridable, `intEnv` truncates+falls-back); construction/origin/warranty have NO wheel-feed source, so the specs grid reads admin metadata if present else hides the row (no fabricated "—"). WB-030: storefront `normalizeFinish` extracted to `@lib/fitment`, both copies guarded by a shared `fixtures/finish-normalize-golden.json` (22 vectors incl. precedence-collision) — drift now fails CI. Subagent-driven (5 TDD tasks, per-task spec+quality reviews, opus final review "ready to merge"). storefront 75 tests / backend 230. **Deferred to pre-deploy:** live-backend smokes (BLANK chip absent; empty specs hidden) — data-shape confirmation, already backstopped by unit tests.
- **Wheel-size fitment hardening (WB-007/008/019/020/043): DONE.** Fractional hub bore stored as scaled-int `hub_bore_mm_x100` (no snapshot drift); fitment cache is TTL-aware with stale-while-revalidate + a nightly warm cron (`wheel-size-warm`, `0 3 * * *`); client requests are AbortController-timeout-bounded (slow/down API → 503, not a hang); the daily quota counter is an atomic upsert-increment (runtime-verified vs the live DB); gated live-slug test added. Built subagent-driven (per-task spec+quality reviews + opus final review). On branch `feat/wheel-size-fitment-hardening`; the non-destructive bore rename+scale migration + the warm cron activate on next deploy.
- **WB-051 — six-axis wheel variant model: DONE + live on prod.** Center bore + load rating became real variant axes so SKUs differing only on those import as distinct variants instead of failing the whole group on an axis collision. Apply now dedupes exact duplicates (in-stock-first) instead of throwing; PDP gained progressive-disclosure bore/load selectors (load cascades off bore, fallback chain prevents unresolvable picks). Full prod wipe + re-import: **groups=2670 variants=29435 errors=0**. Merged to `main` (unpushed); 10 feature commits + 2 migration ops tools (`purge-products` admin route, `vendor-sync-truncate-state.ts`). Built via subagent-driven dev (per-task reviews + final opus whole-branch review).
- On branch `fix/deploy-build-typescript-errors` (unmerged): **WB-050 — deploy build fixed.** `medusa build` was failing on 16 pre-existing TypeScript errors (the backend typechecks at build, unlike the storefront) — this, not the deploy-config work, is what broke every Railway deploy for ~a month (proven by A/B vs commit 786ac54). Fixed all 16 (tsc 0 errors, 253 tests still pass) + switched railway.json to Railpack (Nixpacks hit a config-load `null.admin` before compile). User to re-run the Railway deploy to confirm green.
- Merged to `main`: **Session 2 — WB-016 bounded partial-apply retry** shipped — `partially_failed`/`exhausted` statuses, RunDate short-circuit honors them, `apply_attempt_count` + `applyMaxAttempts` bound, idempotent adopt-by-`external_id`/SKU, shared `finalizeApply`, migration. 4 new pure/seam helpers + 16 Jest cases; full backend suite 253 pass / 4 skipped.
- Merged to `main`: **Session 1 — Deploy & config hardening** — **WB-027** (devMaxRows opt-in), **WB-039** (CORS fail-loud-in-prod), **WB-010** (startup module status), **WB-040** (per-app railway.json). Earlier: **WB-049** (secrets-to-stdout removed), **WB-041** (fail-loud feed guard); **WB-001/002/003/009** verified done.
- **Deploy readiness:** all four 2026-06-05 NO-GO blockers now resolved (WB-002, WB-003, WB-049, WB-016) and WB-041 closed → **backend is deploy-ready** (live boot-against-DB smoke for WB-016's migration recommended post-merge).
- **TIRES (G6 · WB-005) — CODE-COMPLETE across all 3 sub-projects; NEXT = the prod cutover.** The tire store (spec: [tire-store design](in-progress/specs/2026-07-02-tire-store-design.md); scope = full shoppable journey, **tire fitment + MAP pricing out of scope**) was built as three sub-projects, each design → spec → plan → subagent-driven → opus final review: **SP1** backend grouping+indexing (Brand+model, size axis, `product_type = "tire"` Meili) — merged; **SP2** the `/tires` faceted discovery surface (parallel `tire-discovery` module) — merged; **SP3** the tire PDP (shared route branches on a `kind` discriminant → rim-chips-gate-size selector → add-to-cart) — on `feat/tire-pdp`. The wheel surfaces stayed untouched throughout (parallel modules + a thin discriminant seam). **The single remaining step is the prod cutover** — deploy → backend restart (Meili tire settings) → `vendor-sync:dry-run wheelpros-tires` + apply; that both (a) runs SP1's deferred live integration proof and (b) makes `/tires` + tire PDPs render real data (no tire handle resolves until tire products exist). After the cutover, flip WB-005 → done. The rest of G6 (pricing markup/MAP WB-024, de-hardcode bootstrap identity + vendor roster WB-025/026) follows.
- **Other open groups (after tires):** G1 vendor-sync async/scale ✅ DONE 2026-07-06 (see above). G8 remainder = WB-031 (seed shipping + reply-to, general commerce). Quick wins any time: WB-024..WB-026 (pricing/de-hardcode, G6 remainder), WB-054 (gift cards v2), WB-055 (brand-copy sweep), WB-057 (newsletter hardening), WB-058 (plate→YMM provider), WB-031.
- **Pending deploy actions (`main` pushed 2026-07-04 — Railway redeploy auto-triggers on push):** confirm these activate post-deploy — a **Meili re-sync / backend restart for tire `fit_specs`** (the WB-068 `/tires` multi-axis post-filter treats empty `fit_specs` as PASS until then) + a **storefront rebuild** (picks up the tire visual-parity + fit-chip work and any `NEXT_PUBLIC_*` flags). Earlier items are likely already live from the 2026-06-27 WB-059 redeploy — the wheel-size fitment migration + warm cron (2026-06-23), the WB-053 Meili `maxTotalHits` re-sync, and the **G4 newsletter table migration `Migration20260626120000`** (runs on `db:migrate`). G4's Featured Blocks honor `NEXT_PUBLIC_FEATURED_HANDLES` (unset → top-priced fallback) — a `NEXT_PUBLIC_*`, so changing it needs a storefront rebuild. The `NEXT_PUBLIC_EXPRESS_PAY_ENABLED`/`NEXT_PUBLIC_AFFIRM_ENABLED` flags stay off (default) until a provider is wired.
- **WB-059 cutover — DONE (2026-06-27), live & verified.** Ran the purge → truncate-state → re-import → backend-restart sequence; catalog collapsed to 1,724 groups / 29,445 variants / 0 errors. **Because this required a backend redeploy, the other "pending deploy" items above almost certainly activated in the same deploy** — i.e. the wheel-size migration + warm cron, the G4 newsletter table `Migration20260626120000`, and the WB-053 Meili re-sync. Worth a quick confirm they're live (newsletter signup persists; `/store` paginates past 84 pages; nightly `wheel-size-warm` cron registered).

## Map
- Shipped: [done/](done/) · Drafts: [future/](future/) · Living refs: [reference/](reference/) · Backlog: [future/BACKLOG.md](future/BACKLOG.md)
