# WB-113 Sub-model vehicle selector — Implementation Plan

> REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Backend `wheel-size` + storefront YMM. Spec: [../specs/2026-07-17-wb-113-submodel-selector-design.md](../specs/2026-07-17-wb-113-submodel-selector-design.md).

**Global constraints:** Backend tests `cd backend && npx jest src/modules/wheel-size` (`test:fitment` subset) + `npx medusa build` (exit 0). Storefront `npx vitest run <path>` (no globals — `import {describe,it,expect} from "vitest"`), tsc **2-error baseline**, `npx next build`, `npx next lint`. Branch `feat/wb-113-submodel-selector`. `pnpm` may not be on PATH — use `npx`. A backend dev server may be on :9000; if `next build` needs it and it's down, `cd backend && npx medusa develop`. **The wheel-size API is quota-metered** — the live probe is a GATED test (`RUN_WHEEL_SIZE_LIVE=true`), run it sparingly. Order: backend (Tasks 1-3) → storefront (Task 4) so the store-route response shape is settled before the storefront consumes it. Keep the existing `normalizeByModel` union/bore-disagree logic intact — this feature FILTERS the entries fed to it, it does not change normalization.

---

### Task 1: backend pure core + the live probe
**Files:** `backend/src/modules/wheel-size/types.ts` (~51 `RawByModelEntry` + the modifications entry type — add `trim_levels?: string[]`); new `backend/src/modules/wheel-size/sub-models.ts` (two pure fns) + `__tests__/sub-models.test.ts`; extend the gated `__tests__/live-slug.test.ts`.
- [ ] Failing test: pure `subModelsForModelYear(entries)` → deduped, first-seen-order union of `trim_levels` across entries; Corolla-shape (`[{trim_levels:["LE Eco"]},{trim_levels:["L","LE","XLE","SE","XSE"]}]`) → `["LE Eco","L","LE","XLE","SE","XSE"]` (or a documented sort — pick first-seen and pin it); all-empty/none → `[]`. And pure `filterEntriesBySubModel(entries, subModel)` → entries whose `trim_levels` includes `subModel`; `subModel === "Base"` or `undefined`/`""` → ALL entries; a sub-model in 2 entries → both entries (so the caller unions them); no match → `[]` (caller treats as Base/all, see Task 2 — but the pure fn returns the literal match set).
- [ ] RED → implement the two pure fns in a plain module (not a Medusa service). Add `trim_levels?: string[]` to `RawByModelEntry` + the `/modifications` entry shape.
- [ ] **Live probe (gated `RUN_WHEEL_SIZE_LIVE`):** add cases confirming `trim_levels` is present on BOTH `/modifications` AND `/search/by_model/` for: Corolla 2019 (baseline), a truck with OVERLAPPING trims across engines (e.g. Silverado — WT/LT/LTZ under gas+diesel), and a vehicle likely to have EMPTY trim_levels (report which you found). Assert the union is non-empty for the first two. Report the real data in the task report (this de-risks the whole feature).
- [ ] GREEN `npx jest src/modules/wheel-size` (non-live); run the live subset once, report results. Commit `feat(WB-113): sub-model union + entry-filter helpers, trim_levels type, live probe`.

---

### Task 2: backend service — resolve by sub-model + cache + reverse consistency
**Files:** `backend/src/modules/wheel-size/service.ts` (`resolveByModel` ~238-299, `listModifications` ~318-342); `backend/src/modules/wheel-size/cache-key.ts` (~12-20); `backend/src/modules/wheel-size/reverse-fitment.ts` (`extractVehicleIdentity` ~42-61); `normalize.ts` (unchanged internally — confirm). Tests in `__tests__/`.
- [ ] **Read the cache layer FIRST** (how `resolveByModel` caches — does the `wheel_size_fitment` row store the RAW `by_model` data[] or the NORMALIZED result?). Decide per spec step 6: raw-stored → cache at make/model/year (drop sub-model from the key), filter+normalize at read (fewer quota calls); normalized-stored → key the row by sub-model (`cache-key.ts` slot: modificationSlug → subModel). **Report which the layer does + which path you took.**
- [ ] Failing test: `resolveByModel(make,model,year, subModel)` → fetches broad `by_model` (no `modification` param), runs `filterEntriesBySubModel`, then `normalizeByModel` over the subset; a sub-model spanning 2 entries unions their bolt patterns (+ bore-disagree→null still works); `subModel="Base"`/absent → all entries (today's behavior, unchanged). Mock the client; assert the filtered set reaches normalize. Cache-key test updated for the chosen strategy.
- [ ] RED → implement: `resolveByModel` gains a `subModel` param + the filter step; `listModifications` returns `subModelsForModelYear(...)` (the union) instead of the engine `{slug,name}[]`. `extractVehicleIdentity` (reverse path) surfaces `trim_levels` for the "confirmed models" trim display, keeping WB-104's honesty rule (show a sub-model only when the cached entries agree). No `normalizeByModel` internal change.
- [ ] GREEN `npx jest src/modules/wheel-size`; `npx medusa build` exit 0. Commit `feat(WB-113): resolve fitment by sub-model (filter by trim_levels), cache + reverse consistency`.

---

### Task 3: backend store routes — the contract
**Files:** `backend/src/api/store/vehicle-catalog/modifications/route.ts` (response → the sub-model union); the fitment-resolve store route (the one behind `resolveFitmentForVehicle` — find it under `backend/src/api/store/fitment/` or `vehicle-catalog/`, the `modification`/`modificationSlug` query param → `sub_model`). Tests if the routes have them, else the medusa build + a live curl is the gate.
- [ ] Failing test: none if the routes are thin pass-throughs (typical Medusa route) — the gate is `medusa build` + the shape. If there's route-level logic, unit it.
- [ ] Implement: `/store/vehicle-catalog/modifications` returns `{ subModels: string[] }` (the union from `listModifications`). Keep the route PATH (`modifications`) to minimize churn, OR rename to `sub-models` — pick one and note it (the storefront data layer is the only caller, Task 4 follows). The fitment-resolve route accepts a `sub_model` query param (was `modification`) and threads it to `resolveByModel`. **Base**: the storefront sends `sub_model=Base` for the fallback → the route/service treats it as all-entries.
- [ ] `npx medusa build` exit 0. Live-curl (backend :9000): `/store/vehicle-catalog/modifications?make=toyota&model=corolla&year=2019` → the sub-model union; the resolve route with `sub_model=LE` → a fitment with bolt `5x100`. Commit `feat(WB-113): store routes return sub-model union + accept sub_model param`.

---

### Task 4: storefront — the selector, mandatory, Base fallback, vehicle store
**Files:** `storefront/src/lib/data/fitment.ts` (~9-10 `getModifications` → `getSubModels` returning `string[]`; `resolveFitmentForVehicle` param modificationSlug → subModel); `storefront/src/modules/search/components/search-drawer/find-by-vehicle/ymm-pane.tsx` (the 4th select ~169-191/369-389, `canSubmit` ~193, `required`, `resolveFitmentForVehicle` call ~217); `to-options.ts` (the sub-model list is now `string[]`, not `{slug,name}[]` — value === label === the sub-model string); the vehicle store / `Vehicle` type (persist sub-model) + `vehicle-data.ts` `TRIMS_BY_MODEL` fallback (keep). Tests.
- [ ] Failing test: a pure helper for the select options (`string[]` + empty → a single `Base` option) and/or the "unrecognized stored trim → Base" normalization; assert the select is `required`-gated (canSubmit includes the sub-model). Mirror the existing ymm-pane test patterns.
- [ ] RED → implement: the 4th `<select>` renders the sub-model union (value = the string); `required` + joins `canSubmit`; empty union → a single `Base` option (value `"Base"`). `getSubModels` returns the union; `resolveFitmentForVehicle` sends the sub-model string (or `"Base"`). The saved vehicle persists the sub-model; a localStorage vehicle with the OLD shape (an engine slug where a sub-model is expected) is treated as `Base`/prompts re-select — never errors. Engine `name` no longer appears anywhere in the picker.
- [ ] GREEN vitest; `tsc` at/below baseline; `next build`; `next lint`. Commit `feat(WB-113): mandatory sub-model select + Base fallback + vehicle-store persistence`.

---

### Task 5: chunk review + live smoke
- [ ] `scripts/review-package <base> HEAD` → an **opus** reviewer (fitment correctness — a wrong filter/union means the wrong bolt pattern, which mis-sells wheels). Focus: (a) `filterEntriesBySubModel` + `subModelsForModelYear` correct (union, dedup, Base→all, spanning-entries→union); (b) `resolveByModel` feeds the FILTERED subset to `normalizeByModel` and the bore-disagree→null still fires across a multi-entry sub-model; (c) the cache decision is sound (no stale-row over-serve; the chosen key/strategy can't collide two different sub-models); (d) the store-route contract matches what the storefront sends (`sub_model`, `Base`); (e) the select is genuinely mandatory (canSubmit) + the Base fallback + the old-localStorage-shape path can't error or resolve the wrong vehicle; (f) reverse-fitment stays WB-104-honest; (g) engine names are fully gone from the picker. **Live smoke (backend :9000 + next start): Toyota→Corolla→2019 → the select shows L/LE/LE Eco/SE/XLE/XSE, blocks submit until picked, and resolves 5x100; a no-trim vehicle shows Base + resolves.** Report the smoke evidence.
